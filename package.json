{
  "name": "iplay",
  "private": true,
  "version": "0.0.1",
  "description": "Just a start for the new Imperial Portal:-)",
  "repository": "https://github.com/vyable/imperial",
  "dependencies": {
    "body-parser": "^1.9.0",
    "express": "~4.9.0",
    "express-jwt": "~0.2.1",
    "jsonwebtoken": "~0.4.0"
  },
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8080 -c-1"
  }
}

